"""
LLM prompts for transcript analysis.
"""

# ─── Live Streaming Correction (every chunk) ────────────────────────────────

STREAMING_CORRECTION_PROMPT = """Ты — модуль обработки живой речи. Твоя задача — мгновенно очищать текст,
поступающий из системы распознавания (ASR).

Инструкция:
1. Исправь грамматику и логические связки «на лету».
2. Если фраза обрывается, НЕ додумывай её. Исправь только то, что уже сказано.
3. Очисти текст от повторов (например, «я я я думаю» → «я думаю»).
4. Исправь специфические термины (строительные расчеты, Eurocode, ETABS и т.д.).
5. Если есть метки спикеров [Speaker N] или [Имя] — сохрани их.
6. Если имя/термин был НЕПРАВИЛЬНО распознан ранее, но ПРАВИЛЬНО распознан в контексте —
   исправь ВСЕ вхождения.

Формат: Выдавай ТОЛЬКО чистый текст. Без вводных фраз, без пояснений.

КОНТЕКСТ ПРЕДЫДУЩЕГО ТЕКСТА (для связности):
{context}

СЫРОЙ ФРАГМЕНТ ДЛЯ ОЧИСТКИ:
{input_text}

ЧИСТЫЙ ТЕКСТ:"""


# ─── Batch / File Mode — Diarization + Structuring ──────────────────────────

BATCH_DIARIZATION_PROMPT = """Ты — эксперт по расшифровке аудиозаписей.

Задача: Проанализируй загруженный текст транскрибации.

1. **Диаризация**: Раздели текст на разных спикеров. Используй контекст, чтобы понять,
   кто задает вопросы, а кто отвечает. Обозначай их как [Спикер 1], [Спикер 2]
   или именами, если они прозвучали в разговоре.
2. **Структурирование**: Разбей монотонный текст на логические абзацы.
3. **Коррекция**: Исправь ошибки распознавания, превратив «кашу» из слов
   в профессиональный протокол встречи.

Формат вывода — каждая реплика на отдельной строке:
[Имя/Спикер]: Текст реплики

ТЕКСТ ДЛЯ ОБРАБОТКИ:
{transcript}

СТРУКТУРИРОВАННЫЙ ДИАЛОГ:"""


# ─── Post-Edit Mode — Final Cleanup ─────────────────────────────────────────

POST_EDIT_PROMPT = """Перед тобой готовая транскрибация разговора.

Задача:
1. Исправь все ошибки распознавания речи
2. Проверь правильность написания всех технических терминов
3. Убери дублирующиеся фразы
4. Улучши связность текста
5. Сохрани метки спикеров [Имя/Спикер N]
6. Если имя участника было НЕПРАВИЛЬНО распознано в начале, но ПРАВИЛЬНО позже —
   исправь ВСЕ предыдущие вхождения

ТЕКСТ:
{transcript}

ИСПРАВЛЕННЫЙ ТЕКСТ:"""


# ─── Dialogue Reconstruction ────────────────────────────────────────────────

DIALOGUE_PROMPT = """Ниже транскрипт разговора. Идентифицируй и раздели реплики каждого участника.

Используй имена участников, если они встречаются в контексте разговора.
Если имя не упоминается, используй роль (ведущий, эксперт, клиент) или [Спикер N].

Опирайся на контекст: кто задает вопросы, кто отвечает, кто вступает с короткими комментариями
(«да», «согласен», «понятно»).

ТРАНСКРИПТ:
{transcript}

Формат вывода — каждая реплика на отдельной строке:
[Имя/Роль]: Текст реплики

Если текст на иностранном языке, после каждой реплики добавь перевод на русский:
[Имя/Роль]: Текст реплики
→ Перевод реплики на русский

ДИАЛОГ:"""


# ─── Summary ────────────────────────────────────────────────────────────────

SUMMARY_PROMPT = """Проанализируй транскрипт разговора и создай структурированное резюме.

ТРАНСКРИПТ:
{transcript}

Создай резюме со следующей структурой:
1. **Краткое содержание** (2-3 предложения)
2. **Основные темы обсуждения**
3. **Ключевые решения** (если были приняты)
4. **Открытые вопросы** (если остались)

Резюме:"""


# ─── Action Items ───────────────────────────────────────────────────────────

ACTION_ITEMS_PROMPT = """Извлеки все задачи и action items из транскрипта разговора.

ТРАНСКРИПТ:
{transcript}

Для каждой задачи укажи:
- Описание задачи
- Ответственный (если упоминается)
- Срок (если упоминается)
- Приоритет (высокий/средний/низкий)

Формат ответа — JSON:
{{
  "action_items": [
    {{
      "task": "описание",
      "assignee": "имя или null",
      "deadline": "срок или null",
      "priority": "high/medium/low"
    }}
  ]
}}

JSON:"""


# ─── Key Points ─────────────────────────────────────────────────────────────

KEY_POINTS_PROMPT = """Выдели ключевые пункты из транскрипта.

ТРАНСКРИПТ:
{transcript}

Выдели 5-10 самых важных пунктов. Каждый пункт должен быть:
- Конкретным
- Информативным
- Кратким (1-2 предложения)

Ключевые пункты:
1."""


# ─── Topics ─────────────────────────────────────────────────────────────────

TOPICS_PROMPT = """Определи основные темы разговора.

ТРАНСКРИПТ:
{transcript}

Верни список тем в формате JSON:
{{
  "topics": [
    {{
      "name": "название темы",
      "time_percentage": 25,
      "key_points": ["пункт 1", "пункт 2"]
    }}
  ]
}}

JSON:"""


# ─── Translation ────────────────────────────────────────────────────────────

TRANSLATE_FULL_PROMPT = """Переведи следующий текст на русский язык.
Важно: сохрани ТОЧНО такое же количество предложений.
Каждому предложению оригинала должно соответствовать одно предложение перевода.
Верни ТОЛЬКО перевод, без пояснений и комментариев.

ТЕКСТ:
{transcript}

ПЕРЕВОД:"""


# ─── Self-Healing Correction (periodic, accumulated) ────────────────────────

CORRECTION_PROMPT = """Ниже — необработанная транскрипция, полученная по частям из Whisper API.
Между частями могут быть пробелы, обрывы слов, повторы или потерянные фрагменты.
Текст может содержать метки спикеров в формате [Speaker N]: текст.

Твоя задача:
1. Исправь очевидные ошибки распознавания речи
2. Восстанови слова, которые были обрезаны на границах фрагментов
3. Убери дублирующиеся фразы (от перекрытия фрагментов)
4. Улучши связность текста, сохраняя оригинальный смысл
5. НЕ добавляй информацию, которой не было в оригинале
6. ОБЯЗАТЕЛЬНО сохрани метки спикеров [Speaker N] в каждой строке. Формат: [Speaker N]: текст
7. Если имя, название или термин был НЕПРАВИЛЬНО распознан в начале текста,
   но ПРАВИЛЬНО распознан позже — исправь ВСЕ предыдущие неправильные вхождения.
   Например: если вначале "Алекс", а позже ясно что "Алексей" — замени все "Алекс" на "Алексей".
8. Идентифицируй спикеров по именам из контекста. Если имя прозвучало — используй его
   вместо [Speaker N]. Например: [Speaker 1] → [Алексей], если имя ясно из контекста.

НЕОБРАБОТАННАЯ ТРАНСКРИПЦИЯ:
{transcript}

ИСПРАВЛЕННАЯ ТРАНСКРИПЦИЯ:"""
